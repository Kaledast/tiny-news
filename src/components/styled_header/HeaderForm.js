import styled from 'styled-components';

const HeaderForm = styled.form`
  display: flex;
  align-self: flex-end;
  margin-bottom: 9px;
  background: white;
  & input {
    max-width: 130px;
  }
`;

export default HeaderForm;
