import styled from 'styled-components';

const HomeButtonContainer = styled.nav`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-evenly;
  width: auto;
  height: 50%;
  margin-top: 50px;
`;

export default HomeButtonContainer;
